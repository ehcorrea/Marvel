export type Creators = {
  available: number;
  items: {
    name: string;
    role: string;
  }[];
};

export type Characters = {
  available: number;
  items: {
    name: string;
  }[];
};

export type Stories = {
  available: number;
  items: {
    name: string;
    type: string;
  }[];
};

export type Events = {
  available: number;
  items: {
    name: string;
  }[];
};

export type Series = {
  available: number;
  items: {
    name: string;
  }[];
};
